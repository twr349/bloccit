<!DOCTYPE html>
<html lang="eg">
<head>
  <title>Rant</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
  <body>
    <nav class="navbar navbar-inverse">
      <div class="container-fluid">
        <div class="navbar-header">
           <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
             <span class="icon-bar"></span>
             <span class="icon-bar"></span>
             <span class="icon-bar"></span> 
           </button>
           <div class="navbar-brand rant">
             <%= link_to 'RANT', root_path %>   
           </div> 
         </div>
         <div class="collapse navbar-collapse" id="myNavbar">
           <ul class="nav navbar-nav navbar-right">
             <% if current_user %>
               <li><h4 class="rant"><%= link_to "RANTS", topics_path %></h4></li>
               <li><%= link_to ( image_tag (current_user.avatar_url(32)), class: "img-circle"), user_path(current_user) %></li>
               <li><h4 class="rant"><%= link_to "Sign Out", session_path(current_user), method: :delete %></h4></li>
             <% else %>
               <li><h4 class="rant"><%= link_to "Rants", topics_path %></h4></li>
               <li><%= link_to "", new_user_path, class: "glyphicon glyphicon-user" %></li>
               <li><%= link_to "", new_session_path, class: "glyphicon glyphicon-log-in" %></li>
             <% end %>
           </ul>
          </div>
       </div>
   </nav>  
   <% if flash[:notice] %>
     <div class="alert alert-success">
       <button type="button" class="close" data-dismiss="alert">&times;</button>
       <%= flash[:notice] %>
     </div>
   <% elsif flash[:alert] %>
     <div class="alert alert-warning">
       <button type="button" class="close" data-dismiss="alert">&times;</button>
       <%= flash[:alert] %>
    </div>
  <% end %>
  <div class="container">
    <%= yield %> 
  </div>
  
 
</body>
</html>
